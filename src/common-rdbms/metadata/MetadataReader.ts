/**
 * Helps to register metadata collected from different sources.
 */
export class MetadataReader {

    static registerColumn(properties: {
        object: Object,
        propertyName: string,
        columnType: string|undefined,
        columnOptions: Object|undefined
    }) {
    }

}